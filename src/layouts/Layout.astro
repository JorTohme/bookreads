<!DOCTYPE html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<meta name="description" content="Revisa el top de libros vendidos en el mundo actualizado cada día y un análisis de las ventas.">
		<link rel="manifest" href="/manifest.json">
		<link rel="icon" href="/favicon.ico" />
		<link rel="apple-touch-icon" href="/apple-logo.svg">
		<title>Bookreads</title>
	</head>
	<body>
		<slot />
	</body>
</html>
<style is:global>
	:root {
		--accent-gradient: linear-gradient(45deg, rgb(58, 2, 156), #ae4ae3 30%, white 60%);
	}
	
	html {
		font-family: system-ui, sans-serif;
		background-color: #f0f0f0;
	}

	*, *::after, *::before {
		box-sizing: border-box;
	}

	.scrollx::-webkit-scrollbar-track {
		-webkit-box-shadow: none;
		background-color: transparent;
		border-radius: 0;
		height: 12px;
	}

	.scrollx::-webkit-scrollbar
	{
		height: 12px;
		background-color: transparent;
		border-radius: 0 5px;
	}

	.scrollx::-webkit-scrollbar-thumb
	{
		height: 12px;
		border-radius: 0 5px;
	}

	.scrollx::-webkit-scrollbar-button:single-button
	{
		background-color: rgb(0, 77, 172);
		border-radius: 0 5px ;
		display: block;
		height: 13px;
		width: 20px;
	}

	::-webkit-scrollbar-track
	{
		-webkit-box-shadow: inset 0 0 6px rgba(0,0,0,0.3);
		background-color: #F5F5F5;
		border-radius: 10px;
	}

	::-webkit-scrollbar
	{
		width: 10px;
		background-color: #F5F5F5;
	}

	::-webkit-scrollbar-thumb
	{
		border-radius: 10px;
		background-image: -webkit-gradient(linear,
											left bottom,
											left top,
											color-stop(0.44, rgb(122,153,217)),
											color-stop(0.72, rgb(73,125,189)),
											color-stop(0.86, rgb(122,153,217)));
	}
</style>
